import React from 'react';
import styles from './Logo.module.less';

const Logo = () => (
    <div className={`${styles.logo} display-flex flex-center`}>
        {/* <p style={{fontSize: 20, color: 'pink', margin: 0,}}>恩爱唐朝人</p> */}
    </div>
)

export default Logo;